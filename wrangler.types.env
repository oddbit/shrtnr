# Intentionally empty. `yarn types` passes this file as --env-file so that
# `wrangler types` does not read the developer's .dev.vars: the generated
# worker-configuration.d.ts then describes only wrangler.jsonc, and is the
# same on every machine and in CI. Dev-only vars are declared in src/types.ts.
